server:
    port: 7209

springdoc:
    api-docs:
        path: /ifcterrain/v3/api-docs
    swagger-ui: 
        path: /ifcterrain
        operationsSorter: method
        disable-swagger-default-url: true

logging:
    file:
        name: /var/log/ifcterrain-service.log

management:
    endpoint:
        logfile:
            external-file: /var/log/ifcterrain-service.log